name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.0'

      - name: Generate Gradle Wrapper
        run: gradle wrapper --gradle-version 8.0

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
       run: ./gradlew assembleDebug --stacktrace --info
```
4. Commit changes

---

But most likely the problem is your **files are uploaded in the wrong structure**. 

Can you go to your GitHub repo and tell me exactly what you see? It should look like this:
```
📁 .github/
📁 app/
📁 gradle/
📄 build.gradle
📄 gradle.properties
📄 settings.gradle
📄 proguard-rules.pro
```

Specifically — do you see an **`app` folder** in the repo? Click on it and tell me what's inside. It should contain:
```
📁 src/
📄 build.gradle
📄 proguard-rules.pro

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: UnitConverter-APK
          path: app/build/outputs/apk/debug/app-debug.apk
